# Keep Gradle wrapper JAR in git (required for CI)
!gradle/wrapper/gradle-wrapper.jar

# Release keystore (generated in CI, cached — do not commit secrets)
release.keystore
*.jks
.gradle/
build/
app/build/
local.properties
*.iml
.idea/
.DS_Store
